# Spec 模板（v1.0）

> **用途**：Stage 2（Spec）的空白 12 章节模板。
> **怎么用**：复制本文件 → 填 12 个章节 → 另存为 `<project>_spec_v<version>_<date>.zh.md` 到本文件夹。
> **前置条件**：Stage 1（PRD）必须先签字。见 [`../01-prd/checklist_v1.0_zh.md`](../01-prd/checklist_v1.0_zh.md)。
> **关联**：[English template](template_v1.0_en.md)

---

# Spec：<项目 / 功能名>

> **版本**：v1.0
> **日期**：YYYY-MM-DD
> **作者**：<你的名字>
> **Positioning Memo**：<链接>
> **PRD**：<链接>
> **状态**：草稿 | 评审中 | 已批准 | 已弃用

---

## §1 Overview

<一句话：系统做什么。>

**消费者**：<谁调用 —— 用户、其他系统、定时任务>

**系统上下文**：

```
[ASCII 或 mermaid 图，展示本系统 + 依赖 + 消费者]
```

---

## §2 Goals

- **G1**：<可量化目标，如"搜索接口 p99 延迟 < 200ms">
- **G2**：<可量化目标>
- **G3**：<可量化目标>
- **G4**：<可量化目标>
- **G5**：<可量化目标>

**引用**：Positioning 的 UNDERLYING LOGIC —— <link to Positioning Memo §4>

---

## §3 Non-Goals

- ❌ <不做 X>（因为 <理由>）
- ❌ <不做 Y>（因为 <理由>）
- ❌ <不做 Z>（因为 <理由>）

**引用**：PRD §10 Non-Goals —— <link to PRD §10>

---

## §4 Architecture

### §4.1 组件图

```
[ASCII 或 mermaid 图，展示组件 + 数据流]
```

### §4.2 组件

| 组件 | 职责 | 拥有数据 |
|------|------|----------|
| `<名字>` | <做什么> | <数据所有权> |

### §4.3 数据流

<描述数据如何在组件间流动。引用上面的图。>

### §4.4 部署拓扑

| 组件 | 形态 | 位置 |
|------|------|------|
| `<名字>` | 进程 / 容器 / 外部服务 | <主机 / 云 / 区域> |

---

## §5 Data Model

### §5.1 Entities

#### Entity：`<名字>`

| 字段 | 类型 | 描述 |
|------|------|------|
| `<字段>` | <类型> | <描述> |

**关系**：
- <跟其他 entity 的关系>

**存储**：<DB / 表 / 文件 / 缓存>

### §5.2 状态机（如适用）

#### 状态机：`<名字>`

```
[states] --trigger--> [state]
```

| From | To | 触发 |
|------|-----|------|
| <state> | <state> | <事件> |

---

## §6 API Surface

### §6.1 公开 API

#### `GET /api/<endpoint>`

**请求**：
```json
{ "field": "value" }
```

**响应**（200）：
```json
{ "field": "value" }
```

**鉴权**：<所需 scope / 角色>

**错误**：<见 §7>

---

### §6.2 Admin API（如适用）

<同 §6.1 结构>

### §6.3 内部 API（如适用）

<同 §6.1 结构>

---

## §7 Error Model

### §7.1 错误码分类

| 错误码 | 含义 | HTTP 状态 |
|--------|------|-----------|
| `E_NOT_FOUND` | 资源缺失 | 404 |
| `E_RATE_LIMIT` | 触发限流 | 429 |
| `E_INVALID_INPUT` | 验证失败 | 400 |
| ... | ... | ... |

### §7.2 传播规则

- 所有错误带 correlation ID 记录
- 用户可见错误脱敏内部细节
- 5xx 触发告警（见 §8）
- 可重试 vs 不可重试：<按错误码指定>

### §7.3 用户可见消息

- E_NOT_FOUND → "资源未找到"
- E_RATE_LIMIT → "请求过多，N 秒后重试"
- ...

---

## §8 Failure Modes

| 场景 | 检测 | 恢复 |
|------|------|------|
| <数据库宕> | <健康检查失败> | <返回缓存 / 503> |
| <LLM API 宕> | <超时 / 5xx> | <fallback 到缓存 / 返回 E_LLM_UNAVAILABLE> |
| <磁盘满> | <写失败> | <告警 + 丢弃非必要写入> |
| ... | ... | ... |

---

## §9 Performance Budget

| 操作 | 延迟（p50 / p95 / p99） | 吞吐 | 单次成本 |
|------|------------------------|------|----------|
| <操作> | <ms> / <ms> / <ms> | <req/s> | <$> |

**资源上限**：
- CPU：<cores>
- 内存：<GB>
- 磁盘：<GB>
- 网络：<Mbps>

---

## §10 Security & Privacy

### §10.1 认证

<调用方如何证明身份。如 OAuth2、API key、mTLS、JWT。>

### §10.2 授权

| 角色 | 可访问 |
|------|--------|
| <角色> | <资源 / 动作> |

### §10.3 敏感数据

| 字段 | 类型（PII / 凭证 / 业务关键） | 保护 |
|------|------------------------------|------|
| <字段> | PII | <静态加密 + 日志脱敏> |

### §10.4 审计

- 记录哪些操作：<列表>
- 保留期：<时长>
- 谁可读：<角色>

---

## §11 Open Questions

每项**必须**有决策截止日。说不出截止日，问题就没准备好。

| # | 问题 | 决策截止日 | 影响 |
|---|------|------------|------|
| Q1 | <问题> | YYYY-MM-DD | <§2 / §5 / §6 / Plan / Implementation> |
| Q2 | <问题> | YYYY-MM-DD | <...> |

问题被决定后：
- "做" → 移到相关 section（§2 / §5 / §6），升 Spec 版本
- "不做" → 移到 §3 Non-Goals，从 PRD §10 加链接

---

## §12 References

- **Positioning Memo**：<链接>
- **PRD**：<链接>
- **相关 Spec**：<链接>
- **外部标准**：<驱动设计的 RFC / API 文档>
- **Kanban 卡 / commit**：<链接>

---